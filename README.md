# Facebook Personal Bot (Mirai/GoT Style)

## Setup

1. Facebook se `appstate.json` bana lo (FB session cookies).
2. Folder me `appstate.json` daal do.
3. Dependencies install karo:

```bash
npm install
